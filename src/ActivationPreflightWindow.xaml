<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ui="clr-namespace:SherpaManager.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="SherpaManager.ActivationPreflightWindow"
    Style="{StaticResource {x:Type Window}}"
    Title="Review the switch"
    Width="1000"
    Height="900"
    MinWidth="600"
    MinHeight="420"
    Icon="Assets/SherpaManager.ico"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False"
    Background="{StaticResource WindowBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock x:Name="HeadlineText" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />
        </StackPanel>
        <ui:PagedContent Grid.Row="1" Margin="0,20,0,20">
            <ItemsControl x:Name="SectionsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ui:AdaptiveColumns />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource Surface2Brush}" CornerRadius="8" Padding="12" Margin="0,0,12,12" VerticalAlignment="Top">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4" />
                                <ItemsControl ItemsSource="{Binding Rows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border Width="8"
                                                    Height="8"
                                                    CornerRadius="4"
                                                    Margin="0,6,10,0"
                                                    VerticalAlignment="Top"
                                                    Background="{Binding Accent}"/>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap" Foreground="{Binding Accent}" />
                                                    <TextBlock Text="{Binding Detail}"
                                                        TextWrapping="Wrap"
                                                        Visibility="{Binding DetailVisibility}"
                                                        Foreground="{StaticResource MutedTextBrush}"
                                                        Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:PagedContent>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <CheckBox x:Name="SkipPreviewCheckBox" Content="Do not show this again" VerticalAlignment="Center" HorizontalAlignment="Left" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,0,0">
                <Button Content="Cancel" IsCancel="True" Click="Cancel_Click" Margin="0,0,10,0" />
                <Button x:Name="ProceedButton"
                    Style="{StaticResource PrimaryButton}"
                    Content="Switch now"
                    IsDefault="True"
                    Click="Proceed_Click"
                    ui:Icon.Data="{StaticResource IconPlay}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
